# Inference package for MSTACNN crop type classification
#
# Main classes/functions:
#   inference.predict.CropPredictor  — ensemble predictor
#   inference.predict_from_gee       — GEE JSON → prediction
#   inference.prepare_input          — data preparation utilities
#   inference.demo                   — end-to-end validation
